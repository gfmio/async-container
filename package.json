{
  "name": "async-container",
  "module": "dist/index.mjs",
  "type": "module",
  "scripts": {
    "start:node": "node dist/index.js",
    "start:bun": "bun run src/index.ts",
    "build": "bun build --target node --outdir dist --splitting --format esm src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "ts-node": "^10.9.2"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "inversify": "^6.2.2"
  }
}